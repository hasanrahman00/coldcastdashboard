node_modules
dist
dist-ssr
.env
.env.local
.env.*.local
*.log
npm-debug.log*
.DS_Store
.vite

# local preview config
.claude/

.vercel
.env*
# …but DO commit the public templates + per-mode frontend URLs (no secrets in these):
!.env.example
!.env.development
!.env.production

# Google service-account key — NEVER commit
service-account.json
*.sa.json
*-service-account*.json
