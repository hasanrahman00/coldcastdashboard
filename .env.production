# Production build (npm run build / Vercel) — points at the PROD scraper.
# Frontend URLs only; public (baked into the bundle). No secrets here.
# NOTE: a VITE_API_URL set in the Vercel project env vars OVERRIDES this file —
# pick one source of truth (this file, or Vercel) and keep them in sync.
VITE_SALESNAV_SCRAPER_URL=https://salesnav.coldcast.io
VITE_CORE_URL=https://core.coldcast.io
VITE_POST_SCRAPER_URL=https://linkedinpostscraper.coldcast.io
